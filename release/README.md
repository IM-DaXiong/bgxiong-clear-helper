# 预编译版本（Windows x64）

直接下载并双击运行：

- [`bgxiong-clear-helper.exe`](bgxiong-clear-helper.exe)（约 4.8 MB）

无需安装 Rust。清理系统目录 / 注册表时，建议右键「以管理员身份运行」。

版本与仓库 `Cargo.toml` 中的 `version` 一致。许可证见根目录 [`LICENSE`](../LICENSE)（个人/非商免费，商用需授权）。
